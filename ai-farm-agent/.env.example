# === API ===
ANTHROPIC_API_KEY=sk-ant-COLE_SUA_CHAVE_AQUI

# === Seguranca do Servidor ===
# Gere uma chave unica: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=GERE_UMA_CHAVE_SECRETA_AQUI

# === CORS — Origens permitidas (separar por virgula se mais de uma) ===
ALLOWED_ORIGINS=http://127.0.0.1:5000

# === Token de autenticacao para o SocketIO ===
# Gere um token: python -c "import secrets; print(secrets.token_urlsafe(32))"
AUTH_TOKEN=GERE_UM_TOKEN_AQUI
